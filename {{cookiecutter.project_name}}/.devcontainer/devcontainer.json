{
  "name": "sample-project",
  "build": {
    "dockerfile": "Dockerfile"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "charliermarsh.ruff"
      ]
    }
  },

  "postCreateCommand": "uv sync && uv run pre-commit install"
}